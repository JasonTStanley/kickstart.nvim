"$schema" = 'https://starship.rs/config-schema.json'
add_newline = false
format="""
$username\
$hostname\
$directory\
$git_branch\
$python\
$cmd_duration\
$character"""

[python]
symbol='🐍'

format='[${symbol}${pyenv_prefix}(\($virtualenv\) )]($style)'
